/** Required config for prompts to be skipped */
const appRequiredConfig = {
    applicationLanguage: 'kotlin',
};

const appDefaultConfig = {
    ...appRequiredConfig,
};

const defaultConfig = {
    ...appDefaultConfig,
};

module.exports = {
    appDefaultConfig,
    defaultConfig,
};
